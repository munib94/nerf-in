# Core ML frameworks - Platform-specific backend selection
torch>=2.1.0; sys_platform != 'darwin'
torchvision>=0.16.0; sys_platform != 'darwin'
mlx>=0.28.0; sys_platform == 'darwin'
mlx-nn>=0.1.0; sys_platform == 'darwin'

# NeRF-In specific dependencies
# Video object segmentation (STCN)
opencv-python>=4.8.0
# Image inpainting (MST/LaMa)
lama-cleaner>=1.2.0
# Bilateral solver for depth completion
scipy>=1.11.0
scikit-image>=0.21.0

# Standard dependencies
numpy>=1.24.0
pillow>=10.0.0
matplotlib>=3.7.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.8
tqdm>=4.65.0
tensorboard>=2.13.0

# 3D and geometry
trimesh>=3.21.0
open3d>=0.17.0

# Configuration and logging
omegaconf>=2.3.0
wandb>=0.15.0
hydra-core>=1.3.0

# Evaluation metrics
lpips>=0.1.4
kornia>=0.7.0

# Development and testing
pytest>=7.4.0
black>=23.7.0
isort>=5.12.0
flake8>=6.0.0

# Video processing for STCN
ffmpeg-python>=0.2.0
